c
array_of_integer = number.split(delimiter).map(&:to_i)
array_of_integer = number.split(delimiter).map{&:to_i}
delimiter = ","
exit
number.gsub("\n", ",").split(",").map(&:to_i).sum
number.gsub("\n", ",").split(",").map(&:to_i)
number.gsub("\n", ",").split(",").sum
number.gsub("\n", ",").split(",")
number.gsub("\n", ",").split()
number.gsub("\n", ",")
number.split(",").map{|a| a.to_i}.sum
number = "1,2,3"
number.split(",").map{|a| a.to_i}.sum
number.split(",").map{|a| a.to_i}
number.split(",").map{|a| a.to_i}[0]
number.split(",").map{|a| a.to_i}.flatten
number.split(",").map{|a| a.to_i}
number.split(",")
number.split(delimiter)
number.split(delimiter).map(&:to_i)
number.split(delimiter).map(&:i)
number.split(delimiter)
numbers.split(delimiter)
delimiter = ","
number.split(",")
number.split()
number.empty?
number = "1"
number
number.empty?
